1. Replace i Replace All (rozszerzenie istniejącego Find).
2. Quick Open (`Ctrl+P`) po plikach projektu.
3. Global Search (`Find in Files`) z panelem wyników.
4. Pełniejsze LSP: Go to Definition, Find References, Rename Symbol, Hover.
5. Lepszy Explorer: drzewo rekurencyjne + new/rename/delete.
6. Build profiles (Debug/Release, args, env, target).
7. Integracja debuggera (breakpointy, step, call stack, watch).
8. Terminal: historia (`↑/↓`), wybór shella, clear, restart.
9. Autosave + crash recovery + ostrzeżenia o niezapisanych kartach.
10. Workspace/projects (`.finworkspace`) z layoutem i taskami.
11. Command Palette (`Ctrl+Shift+P`).
12. Git basics: status, diff, stage/unstage, commit.
